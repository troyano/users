server:
  port: 9001

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/users
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: none 
    show-sql: true

appDescription: "Pragma users"
appVersion: "1.0.0"

logging:
  level:
    org:
      springframework:
        security=DEBUG:
